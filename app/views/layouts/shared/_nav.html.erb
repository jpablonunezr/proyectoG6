<header><!-- EDIIIIIIIIIIIIT-->
	<div class="logo" href="<%= root_path %>"></div>
	<% if user_signed_in? %>
	<nav>
		<div class="acc">
			<div class="img-profile" style="background-image:url(<%= current_user.photo %>)"></div>
			<h4><%= current_user.first_name %> <%= current_user.last_name %></h4>
			<h5><%= current_user.email %> </h5>
			<a href="<%= destroy_user_session_path  %>" data-method="delete" class="logout">Cerrar sesión</a>			
		</div>

		
		<p class="copy-right">Todos los derechos reservados.<br>&copy; 2017 Teachub, y teachub.com son marcas registradas.</p>
		<div class="edit-profile">
			<div></div><div></div><div></div>
		</div>	
	

	</nav>


<%= render "devise/registrations/editparcial" %>


	<% else %>

	<nav></nav>

	<% end %>

</header>
